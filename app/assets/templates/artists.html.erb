<h1>Browse Artists</h1>
<p>Here are all the top artists our users have listened to this month.</p>

<div class="col-lg-3 col-md-4 col-xs-6" ng-repeat="artist in artists.all">
    <img id="artists" class="img-responsive img-thumbnail center-block" src="{{ artist.image }}">
      <h1><a href="{{ artist.link }}" target="_blank">{{ artist.name }}</a></h1>
  </div>
  </div>
</div>